<%= form_with model: @todo, local: true do |f| %>
    <%= f.label :description %>
    <%= f.text_field :description %>

    <%= f.label :completed %>
    <%= f.check_box :description %>

    <%= f.submit 'Enviar'%>
<% end %>